let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_151‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_151‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_151‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_151‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_151‍.w("./api",[["default",["api"],function(v){api=v}]]);_151‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();







// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만둘기
const { PORT, MONGO_URI } = process.env;

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes()); // api 라우트 적용

//라우트 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware);

//app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

mongoose
  .connect(MONGO_URI, {
    useMongoClient: true,
  })
  .then((response) => {
    console.log('Successfully connected to mongodb');
  })
  .catch((e) => {
    _151‍.g.console.error(e);
  });

const port = process.env.PORT || 4000;
app.listen(port, () => {
  _151‍.g.console.log('Listening to port %d', port);
});
